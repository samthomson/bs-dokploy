# Leave empty to use the domain from incoming requests
# This works for both local dev and production
publicDomain: ""
databasePath: data/sqlite.db

dashboard:
  enabled: true
  username: "${DASHBOARD_USER}"
  password: "${DASHBOARD_PASS}"

storage:
  backend: local
  local:
    dir: ./data/blobs
  # Rules determine what blobs are accepted and how long they're kept
  # Add your npub(s) converted to hex to restrict storage to specific users
  rules:
    - type: "image/*"
      expiration: never
      # pubkeys:
      #   - "your-hex-pubkey-here"
    - type: "video/*"
      expiration: never
      # pubkeys:
      #   - "your-hex-pubkey-here"
    - type: "*"
      expiration: never

upload:
  enabled: true
  requireAuth: true          # require nostr auth for uploads

media:
  enabled: true
  requireAuth: false         # turn off for quick testing, then re-enable
